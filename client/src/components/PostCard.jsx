import React from "react";

const PostCard = ({ UserName, DateTime, Content }) => {
  return (
    <div className="w-full h-[min-content] bg-white px-3 py-3 cursor-pointer">
      <div> </div>
    </div>
  );
};

export default PostCard;
