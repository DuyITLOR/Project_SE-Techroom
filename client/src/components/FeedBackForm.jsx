import React, { useState } from "react";

const FeedBackForm = ({ ClassName, StudentID, StudentName }) => {
  const [tag, setTag] = useState(0);
  const [content, setContent] = useState("");

  return <div></div>;
};

export default FeedBackForm;
